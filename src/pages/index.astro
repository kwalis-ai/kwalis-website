---
import { execSync } from 'node:child_process';
import BaseLayout from '../layouts/BaseLayout.astro';
import EmailSignup from '../components/EmailSignup.astro';

const gitHash = (() => {
  try {
    return execSync('git rev-parse --short HEAD').toString().trim();
  } catch {
    return 'dev';
  }
})();

const principles = [
  {
    title: 'Context is localized',
    body: 'No global memory blob. Knowledge exists relative to a focus.',
  },
  {
    title: 'Understanding is compositional',
    body: 'Explanations are structural objects with explicit interfaces, not text chains.',
  },
  {
    title: 'Judgment is explicit',
    body: 'Evidence and constraints are first-class and auditable.',
  },
  {
    title: 'Commitment is stabilization',
    body: 'What an agent knows is what remains invariant under admissible change.',
  },
  {
    title: 'Learning is conservative',
    body: 'Assumptions weaken only when necessary, and only as much as required.',
  },
  {
    title: 'Curiosity is principled',
    body: 'Exploration is driven by reducible uncertainty, not arbitrary incentives.',
  },
];
---

<BaseLayout
  title="Kwalis — Semantic Memory for Agentic AI"
  description="Kwalis is building the semantic memory layer for agentic AI. Structured memory that makes agents coherent over time."
>

  <!-- Ambient background -->
  <div class="bg-glow" aria-hidden="true"></div>

  <nav class="hero-enter hero-enter-d1">
    <span class="logo">Kwalis</span>
  </nav>

  <section class="hero">
    <div class="hero-content">
      <p class="hero-eyebrow hero-enter hero-enter-d1">Semantic memory for agentic AI</p>
      <h1 class="hero-enter hero-enter-d2">
        The memory layer that makes AI agents
        <em class="gradient-text">coherent over time.</em>
      </h1>
      <p class="hero-sub hero-enter hero-enter-d3">
        AI agents don't fail because they lack intelligence.<br />
        They fail because they lack memory that preserves meaning.
      </p>
    </div>
  </section>

  <section class="pullquote hero-enter hero-enter-d4">
    <div class="pullquote-rule"></div>
    <div class="phrases">
      <div class="phrases-track">
        <span>The problem isn't scale. It's meaning.</span>
        <span>Autonomy without memory is just automated amnesia.</span>
        <span>A bigger window doesn't fix a broken view.</span>
      </div>
    </div>
  </section>

  <div class="divider"><hr /></div>

  <section class="problem reveal">
    <p class="section-label">The problem</p>
    <h2>
      What we call "memory" in AI today is retrieval, embeddings, and summaries.
      What we call "learning" is fine-tuning or prompt accretion.
      <strong>These break down the moment agents become long-running or autonomous.</strong>
      The core problem is not scale. It is meaning.
    </h2>
  </section>

  <div class="divider"><hr /></div>

  <section class="principles reveal">
    <p class="section-label">What we believe</p>
    <ol class="principles-list stagger">
      {principles.map((p, i) => (
        <li class="principle">
          <span class="principle-num">{String(i + 1).padStart(2, '0')}</span>
          <div class="principle-content">
            <strong>{p.title}</strong>
            <span>{p.body}</span>
          </div>
        </li>
      ))}
    </ol>
  </section>

  <section class="cta reveal">
    <EmailSignup />
  </section>

  <div class="divider"><hr /></div>

  <section class="contact reveal">
    <p>
      We're building the semantic substrate for agentic AI.<br />
      If this resonates, reach out at <a href="mailto:contact@kwalis.ai">contact@kwalis.ai</a>
    </p>
  </section>

  <footer>
    <span>&copy; 2026 Kwalis</span>
    <span class="build-hash" title={`Build: ${gitHash}`}>{gitHash}</span>
  </footer>

</BaseLayout>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.15, rootMargin: '0px 0px -40px 0px' }
  );

  document.querySelectorAll('.reveal, .stagger').forEach((el) => observer.observe(el));
</script>

<style>
  /* --- Ambient background --- */
  .bg-glow {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    background:
      radial-gradient(ellipse 50% 40% at 20% 20%, rgba(212, 165, 116, 0.04) 0%, transparent 70%),
      radial-gradient(ellipse 40% 50% at 80% 60%, rgba(212, 165, 116, 0.025) 0%, transparent 70%);
  }

  /* --- Nav --- */
  nav {
    position: relative;
    z-index: 1;
    padding: 2.5rem 2rem 2rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }

  .logo {
    font-family: var(--font-sans);
    font-size: 1.1rem;
    font-weight: 500;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text);
  }

  /* --- Hero --- */
  .hero {
    position: relative;
    z-index: 1;
    min-height: auto;
    padding-top: 12vh;
    padding-bottom: 3rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 0 2rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }

  .hero-eyebrow {
    font-size: 0.8rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--accent);
    margin-bottom: 2rem;
  }

  h1 {
    font-family: var(--font-serif);
    font-size: clamp(2.6rem, 5.8vw, 4.4rem);
    font-weight: 400;
    line-height: 1.1;
    margin-bottom: 2rem;
    max-width: 740px;
    letter-spacing: -0.01em;
    color: var(--text);
  }

  .gradient-text {
    font-style: italic;
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent-bright) 60%, #e8cba8 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-sub {
    font-size: 1.15rem;
    font-weight: 300;
    color: var(--text-muted);
    max-width: 500px;
    line-height: 1.8;
  }

  /* --- Pullquote --- */
  .pullquote {
    position: relative;
    z-index: 1;
    max-width: var(--max-w);
    margin: 0 auto;
    padding: 2rem 2rem 3rem;
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .pullquote-rule {
    width: 2px;
    height: 2.4rem;
    background: var(--accent);
    border-radius: 1px;
    flex-shrink: 0;
    opacity: 0.6;
  }

  .phrases {
    height: 2.4rem;
    overflow: hidden;
  }

  .phrases-track {
    animation: rotate-phrases 12s ease-in-out infinite;
  }

  .phrases-track span {
    display: block;
    height: 2.4rem;
    line-height: 2.4rem;
    font-family: var(--font-serif);
    font-size: clamp(1.1rem, 2.2vw, 1.4rem);
    font-weight: 400;
    font-style: italic;
    color: var(--text-muted);
  }

  @keyframes rotate-phrases {
    0%, 28%   { transform: translateY(0); }
    33%, 61%  { transform: translateY(-2.4rem); }
    66%, 94%  { transform: translateY(-4.8rem); }
    100%      { transform: translateY(0); }
  }

  /* --- Divider --- */
  .divider {
    max-width: var(--max-w);
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 1;
  }

  .divider hr {
    border: none;
    border-top: 1px solid var(--border);
  }

  /* --- Section label --- */
  .section-label {
    font-size: 0.75rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--accent);
    margin-bottom: 2.5rem;
    opacity: 0.8;
  }

  /* --- Problem --- */
  .problem {
    position: relative;
    z-index: 1;
    padding: 3.5rem 2rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }

  .problem h2 {
    font-family: var(--font-serif);
    font-size: clamp(1.4rem, 2.8vw, 2.1rem);
    font-weight: 400;
    line-height: 1.5;
    max-width: 660px;
    color: var(--text-muted);
  }

  .problem h2 strong {
    color: var(--text);
    font-weight: 400;
  }

  /* --- Principles --- */
  .principles {
    position: relative;
    z-index: 1;
    padding: 3.5rem 2rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }

  .principles-list {
    list-style: none;
    display: grid;
    gap: 0;
  }

  .principle {
    display: flex;
    align-items: baseline;
    gap: 2rem;
    padding: 1.75rem 0;
    border-bottom: 1px solid var(--border);
    transition: border-color 0.3s ease;
  }

  .principle:first-child {
    border-top: 1px solid var(--border);
  }

  .principle:hover {
    border-color: var(--border-hover);
  }

  .principle:hover + .principle {
    border-top-color: var(--border-hover);
  }

  .principle:hover .principle-num {
    opacity: 1;
  }

  .principle-num {
    font-family: var(--font-serif);
    font-size: 1.1rem;
    font-style: italic;
    color: var(--accent);
    flex-shrink: 0;
    padding-top: 0.1rem;
    font-variant-numeric: tabular-nums;
    opacity: 0.5;
    transition: opacity 0.3s ease;
    min-width: 1.8rem;
  }

  .principle-content {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .principle-content strong {
    font-family: var(--font-serif);
    font-size: 1.15rem;
    font-weight: 400;
    color: var(--text);
  }

  .principle-content span {
    font-size: 0.9rem;
    font-weight: 300;
    color: var(--text-muted);
    line-height: 1.6;
  }

  /* --- CTA --- */
  .cta {
    position: relative;
    z-index: 1;
    padding: 3rem 2rem;
    max-width: var(--max-w);
    margin: 0 auto;
  }

  /* --- Contact --- */
  .contact {
    position: relative;
    z-index: 1;
    padding: 3rem 2rem 2rem;
    max-width: var(--max-w);
    margin: 0 auto;
    text-align: center;
  }

  .contact p {
    font-size: 1.05rem;
    font-weight: 300;
    color: var(--text-muted);
    line-height: 1.8;
  }

  .contact a {
    color: var(--accent);
    text-decoration: none;
    font-weight: 400;
    transition: color 0.2s ease;
    border-bottom: 1px solid transparent;
  }

  .contact a:hover {
    color: var(--accent-bright);
    border-bottom-color: var(--accent-bright);
  }

  /* --- Footer --- */
  footer {
    position: relative;
    z-index: 1;
    padding: 2rem;
    text-align: center;
    font-size: 0.8rem;
    color: var(--text-dim);
    display: flex;
    justify-content: center;
    gap: 1rem;
  }

  .build-hash {
    font-family: monospace;
    font-size: 0.7rem;
    opacity: 0.4;
  }

  /* --- Responsive --- */
  @media (max-width: 640px) {
    nav {
      padding: 1.5rem 1.5rem 0;
    }

    .hero {
      padding: 0 1.5rem;
      min-height: 72vh;
    }

    .pullquote {
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }

    .problem,
    .principles,
    .contact {
      padding-left: 1.5rem;
      padding-right: 1.5rem;
    }

    .principle {
      gap: 1.2rem;
    }
  }
</style>
